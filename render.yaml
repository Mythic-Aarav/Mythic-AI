services:
  - type: web
    name: PUT-YOUR-EXISTING-SERVICE-NAME-HERE
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: starter          # match whatever plan your existing service is on
    autoDeploy: true
